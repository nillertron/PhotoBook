<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="PhotobookUploader.View.UploadPicture">
    <ContentPage.Content>
        <StackLayout VerticalOptions="CenterAndExpand">
            <Label Text="{Binding Msg}"></Label>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Text="Pick from library" Command="{Binding ChosePicCommand}"></Button>
                <Button Grid.Column="1" Text="Take with camera" Command="{Binding TakePhotoCommand}"></Button>
            </Grid>
            <Picker SelectedIndex="{Binding SelectedIndex}" ItemsSource="{Binding FotoalbumsListe}" ItemDisplayBinding="{Binding Navn}">
            </Picker>
            <Editor Placeholder="Description" Text="{Binding Beskrivelse}"/>
            <Image Source="{Binding ImgSource}"></Image>
            <Button Text="Upload" Command="{Binding UploadCommand}"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>